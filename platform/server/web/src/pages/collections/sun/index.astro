---
// Route 7 of 25 — 05_ROUTE_TEMPLATE_MATRIX.md. Metadata/H1 come from the
// central registry (src/lib/metadata.ts); marked pendingContent there
// since the specification leaves this title/H1 "*derive — must be
// unique*" (§3), i.e. not yet approved.
import Page from '../../../layouts/Page.astro';
import SkeletonNotice from '../../../components/dev/SkeletonNotice.astro';
import { STATIC_ROUTE_METADATA } from '../../../lib/metadata.ts';
import { staticBreadcrumbs } from '../../../lib/routes.ts';
---

<Page meta={STATIC_ROUTE_METADATA['/collections/sun/']} breadcrumbs={staticBreadcrumbs('/collections/sun/')}>
  <SkeletonNotice />
</Page>
