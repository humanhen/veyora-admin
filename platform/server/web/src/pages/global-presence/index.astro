---
// Route 12 of 25 — 05_ROUTE_TEMPLATE_MATRIX.md. Title/H1 are the
// specification's own documented values (§3). The title's country list is
// the specification's own text, explicitly flagged there as "re-checked
// after ... the domain decision" — carried through as-is, not invented
// here.
import Page from '../../layouts/Page.astro';
import SkeletonNotice from '../../components/dev/SkeletonNotice.astro';
import { staticBreadcrumbs } from '../../lib/routes.ts';
---

<Page
  title="Eyewear Distribution in Canada, USA and Europe | Veyora"
  breadcrumbs={staticBreadcrumbs('/global-presence/')}
>
  <h1>Global eyewear distribution with local support.</h1>
  <SkeletonNotice />
</Page>
