---
// Route 25 of 25 — 05_ROUTE_TEMPLATE_MATRIX.md: the HTML sitemap
// (`HtmlSitemap` template), distinct from the XML sitemaps generated in
// B7. Metadata/H1 come from the central registry (src/lib/metadata.ts).
// The real version groups links from published records that do not exist
// until B2 — this is a neutral structural skeleton.
import Page from '../../layouts/Page.astro';
import SkeletonNotice from '../../components/dev/SkeletonNotice.astro';
import { STATIC_ROUTE_METADATA } from '../../lib/metadata.ts';
import { staticBreadcrumbs } from '../../lib/routes.ts';
---

<Page meta={STATIC_ROUTE_METADATA['/sitemap/']} breadcrumbs={staticBreadcrumbs('/sitemap/')}>
  <SkeletonNotice />
</Page>
