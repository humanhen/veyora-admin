.claude/
.upload-staging/

node_modules/
# package-lock.json is COMMITTED on purpose: the Docker build runs `npm ci`, so
# the lockfile is what makes a rebuild of the same commit reproducible.

# local dev stack (secrets + machine-specific overrides + import scratch)
platform/server/.env
platform/server/docker-compose.override.yml
platform/server/data/
